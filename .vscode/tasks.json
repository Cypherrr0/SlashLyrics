{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "esbuild:watch",
            "type": "shell",
            "command": "npm run esbuild:watch",
            "isBackground": true,
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$esbuild-watch"]
        }
    ]
}
